import{useEffect,useRef}from'react';export function useGlassRefraction(){const ref=useRef(null);useEffect(()=>{const n=ref.current;if(!n)return;const move=e=>{const r=n.getBoundingClientRect();n.style.setProperty('--mx',`${(e.clientX-r.left)/r.width*100}%`);n.style.setProperty('--my',`${(e.clientY-r.top)/r.height*100}%`)};n.addEventListener('pointermove',move);return()=>n.removeEventListener('pointermove',move)},[]);return ref}
