# ROOT design execution index

For current G1 visual execution:

- `stock-asset-pipeline.md` — provider policy, local setup model, search queries, candidate review gate, and visual storytelling rules.
- `asset-agent-brief.md` — Codex assembly directive for using those ingredients inside the existing React/Vite site.

These documents operationalize the existing Clinical Glass direction; they do not replace the broader design-system documentation.
