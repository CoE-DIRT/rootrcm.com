# CLAUDE-STATE — working memory for the DIRT→ROOT merge

Read once: MERGE-LAW.md, CLAUDE-HANDOFF.md (source/target table), DESIGN-TOKEN-MERGE.md, PUBLIC-SAFETY-EXCLUSIONS.md, ROUTE-MERGE-MAP.md. Do not re-open SOURCE-INVENTORY.md / CONFLICT-MATRIX.md unless a specific question needs them.

R = `C:/Users/aasha/OneDrive/Documents/GitHub/CoE-DIRT/rootrcm.com`
D = `C:/Users/aasha/OneDrive/Documents/GitHub/CoE-DIRT/rcm-iaas-netlify-mvp/premium-react-site`
Branch: `feat/final-dirt-root-merge`. DIRT HEAD verified `9bb19ef5...bdf`.

## Phase status
- [x] Phase 1 — token system, GlassCard, Section/Button merge, Header/Footer/Shell, Hero, shared ui (NoPhiBanner/AnnotationNote/StatusPill/ConversionPanel)
- [x] Phase 2 — MetricCard/ResponsiveTableShell, NestedDataGridContainer, DirtCommandCenter recompose, InteractiveMiniDashboard, DirtSectionNav
- [x] Phase 3 — PracticeLifecycle/ContinuousImprovementLoop/VerticalExpansionMap, PipelineCard/InteroperabilityLifecycle, CompetitiveTable
- [x] Phase 4 — DiagnosticPage/ImplementationSteps, PricingPage
- [x] Phase 5 — ServicesPages (12), ContentPages solutions (7)
- [x] Phase 6 — ContentPages case-studies/resources, WorkforceGrid/CompanyPages, TrustSignals/LegalPages
- [x] Phase 7 — cleanup (legacy CSS deliberately left untouched — see IMPLEMENTATION-LEDGER.md "Phase 7"; no App/vite/route parity fixes were needed)
- [x] Phase 8 — QA gates (lint/test/tsc/build/playwright, responsive 390/430/768/1024/1366/1440/1536 all PASS)
- [x] Phase 9 — CURSOR-HANDOFF.md + IMPLEMENTATION-LEDGER.md written
- [x] Phase 10 — local commit (see git log)

## Key facts
- ROOT stack owner of tokens: `src/v4/styles/tailwind.css` (Tailwind 4 `@theme`). Do NOT import D's index.css/tailwind.config.js directly — translate values only (see DESIGN-TOKEN-MERGE.md table).
- All new components: typed TSX under `src/v4/components/...`. Preserve DIRT provenance as a one-line comment `// Ported from DIRT premium-react-site: <path>`.
- Never port: AuthContext, api.js, ProtectedRoute, LoginPage, LiveDashboardPage, NoPhiFormCard/formCatalog, CommercialPages paymentLinks/Checkout, WorkspacePreviewPages, DirtrcmSimulator, TrustModel BAA roadmap, case-study arrays, WorkforceGrid staff/CoE identity, archived front-end-v1/v3.
- Link rewrite table lives in ROUTE-MERGE-MAP.md — apply when porting DIRT internal links.
- Business truth to preserve: Diagnostic $2,500 fixed; Managed RCM from $1,500 onboarding / ~5% collections; DIRT $1,500–$2,500/mo. Never invent customers/testimonials/certifications/integrations.
- No push/PR/merge/deploy at any point.

## Progress log
(append short dated entries as phases complete, newest last)
- 2026-09-14: Phase 6 done. Added `WorkforceGrid.tsx` (components/sections/) — reframed DIRT's fabricated executive job titles (VP of Revenue Strategy, Chief Trust Officer, etc.) as ROOT responsibility-area cards (Revenue strategy / Operating execution / Analytics & reporting / Technology & automation), no invented staff per PUBLIC-SAFETY-EXCLUSIONS. Wired into `CompanyPages.tsx` AboutPage. ContactPage form now in GlassCard, outreach chips pill-styled. `TrustSignals.tsx` cards now use GlassCard variant="trust". `CookiesLegalPage.tsx` disclosure table now uses `ResponsiveTableShell`. `LegalPages.tsx` ThankYouPage/NotFoundPage wrapped in GlassCard (green/amber accent). PrivacyPage/TermsPage legal text left untouched (no restyle risk to legal copy). Gates: tsc/lint/test/build all PASS.
- 2026-09-14: Phase 5 done. `ServicesPages.tsx` (covers all 12 services via shared `ServicePage` template) and `ContentPages.tsx` (covers all 7 solutions via shared `SolutionPage`, 6 resources via `ResourceArticlePage`, case-studies hub) now use GlassCard/MetricCard/AnnotationNote uniformly — one template edit upgrades every route in each family, per MERGE-LAW "one active design system." Fixed a second instance of the gated dirt-poc-01 image bug: `CaseStudiesHubPage` referenced `/assets/case-studies/dirt-poc-01/previews/slide-01-the-problem.png` unconditionally (that study has publicReady:false, stripped from prod build) — now only renders a safe editorial image when `studies.length` is non-empty. Card links fixed to wrap `<a href>` around a non-interactive GlassCard rather than passing `href` to GlassCard's `as` prop (GlassCard's outer wrapper is always a div — `as` only changes the inner content tag, so `href` would have been silently dropped). Gates: tsc/lint/test/build all PASS.
- 2026-09-14: Phase 4 done. Added `ImplementationSteps.tsx` (components/sections/) — connected-timeline visual only, no Day 1/7/30 promises (caller supplies ROOT's actual 4-step journey). `DiagnosticPage.tsx`: hero NoPhiBanner, form wrapped in GlassCard, journey now uses ImplementationSteps, sample cards use GlassCard, `id="sample-report"` correctly on the Illustrative Sample section (not the journey section). `PricingPage.tsx`: Diagnostic panel + model cards now use GlassCard glass/matrix variants. All functional logic (form, checkout gating, attribution, fallback, noPhi acknowledgement) untouched — visual restyle only. Verified live at /diagnostic/ via get_page_text: form fields, journey, sample section all intact. Gates: tsc/lint/test/build all PASS.
- 2026-09-14: Phase 3 done. Added `PracticeLifecycle.tsx`, `ContinuousImprovementLoop.tsx`, `VerticalExpansionMap.tsx`, `CompetitiveTable.tsx` (components/sections/), `PipelineCard.tsx` (components/ui/), `InteroperabilityLifecycle.tsx` (components/sections/) — all with ROOT-only default content (no DIRT siteData/experienceContent copy). VerticalExpansionMap links only to real existing ROOT routes (no tenant/backend/roadmap invention). InteroperabilityLifecycle statuses are Illustrative/Planned only (never "ready"/live). CompetitiveTable reframed as neutral role comparison (ROOT/DIRT/EHR-PM/Clearinghouses) — dropped DIRT's competitor-limitation/best-fit-buyer claims per PUBLIC-SAFETY-EXCLUSIONS. Desktop/mobile table-vs-card switch uses CSS breakpoints (`hidden xl:block`/`xl:hidden`), not a JS useMediaQuery hook (no SSR here, so no hydration concern, and this avoids an extra hook). Wired into `PlatformPage.tsx` (lifecycle+loop+expansion map) and `TechnologyPages.tsx` TechnologyHubPage (interop lifecycle + competitive table). Gates: tsc/lint/test/build all PASS.
- 2026-09-14: Phase 2 done. Added `MetricCard.tsx`, `ResponsiveTableShell.tsx` (components/ui/); `NestedDataGridContainer.tsx`, `InteractiveMiniDashboard.tsx`, `DirtSectionNav.tsx` (components/dirt/); extended `dirtDemo.ts` with `triageMetrics`/`triageRows`/`scenario` fixtures (ROOT-only synthetic data, no DIRT fixture copy). `DirtCommandCenter.tsx` now embeds `NestedDataGridContainer` and uses `ResponsiveTableShell` for the payer table. `/technology/dirt/` (`TechnologyPages.tsx` DirtPage) restructured into orient→signal→command-center→scenario→action with `DirtSectionNav` anchors. `InteractiveMiniDashboard` renamed off "Live"/"Apply Intelligence" framing per PUBLIC-SAFETY-EXCLUSIONS #2. Verified interaction (Explain row expand) via accessibility tree + get_page_text — content renders correctly, no truncation/garbling. Gates: tsc/lint/test/build all PASS.
- 2026-09-14: Phase 1 core done. New token system in `src/v4/styles/tailwind.css` (DIRT hex values under existing var names — additive, no renames, so all existing `bg-panel`/`text-muted`/`text-accent`/etc. utilities repaint automatically). Added `GlassCard.tsx`, `NoPhiBanner.tsx`, `AnnotationNote.tsx`, `StatusPill.tsx`, `ConversionPanel.tsx` under `components/ui/`. Added `HeroSection.tsx`/`HeroMockup.tsx` under `components/sections/`, wired into `HomePage.tsx` (replaced full-bleed photo hero with DIRT split hero + bounded HeroMockup framing the real DirtCommandCenter preview instead of DIRT's fake dollar rows). Fixed PUBLIC-SAFETY-EXCLUSIONS #1 (Home no longer references gated `dirt-poc-01` cover image; uses `mediaAssets.dirtAnalytics`). Extended `Section.tsx` with `tone="grid"|"safe"|"flow"` glass-shell variants and gradient-underline `SectionHeader`. Restyled `Button.tsx` (pill shape, premium easing/hover-lift). Header/Footer got DIRT glass surface treatment (bg-bg-deep/88 backdrop-blur-2xl saturate-150). Fixed hardcoded hex chart colors in `DirtCommandCenter.tsx` to use CSS vars. IMPORTANT: preserved `.homeHero`/`.heroCopy`/`.lede` DOM hooks in HeroSection — `src/experiments.js` `applyPageExperiment` does raw `document.querySelector('.homeHero')` DOM surgery for a live home-hero A/B test; breaking those class names silently breaks the experiment (caught via `npm test`, not typecheck/lint). Gates: tsc/lint/test/build all PASS.
